experiment_name: baseline_ft

data_path: "data/processed/baseline"


model:
  name: resnet50
  pretrained: true

training: #no hyperparameter-tuning
  scratch: false
  ht: false
  layers: 2
  params:
    epochs: 10
    batch_size: 64
    lr: 0.001
  
best_config_run: false #use in hyperparameter tuning

output_dir: results/NEW_classifier_on_baseline_RESNET50/ft