experiment_name: augmented_ft

data_path: "data/processed/augmented"


model:
  name: resnet50
  pretrained: true

training: #no hyperparameter-tuning
  ht: false
  layers: 2
  params:
    epochs: 10
    batch_size: 64
    lr: 0.001
  
best_config_run: false #use in hyperparameter tuning

output_dir: results/classifier_on_augmented/ft