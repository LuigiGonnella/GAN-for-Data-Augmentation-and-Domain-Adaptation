experiment_name: baseline_freeze

data_path: "data/processed/baseline"

model:
  name: resnet50
  pretrained: true

training:
  scratch: false
  ht: false
  layers: false
  params:
    epochs: 10
    batch_size: 64
    lr: 0.001
  
best_config_run: false #use in hyperparameter tuning

output_dir: results/NEW_classifier_on_baseline_RESNET50/freeze
