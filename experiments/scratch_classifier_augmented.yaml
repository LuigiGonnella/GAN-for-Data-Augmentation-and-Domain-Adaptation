experiment_name: augmented_scratch

data_path: "data/processed/augmented"

model:
  name: resnet18
  pretrained: false

training:
  scratch: true
  ht: false
  params:
    epochs: 50
    batch_size: 64
    lr: 0.001
  
best_config_run: false #use in hyperparameter tuning

output_dir: results/scratch_classifier_on_augmented_RESNET18/no_ht
