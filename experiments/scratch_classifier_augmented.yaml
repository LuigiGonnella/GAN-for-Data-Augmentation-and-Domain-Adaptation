experiment_name: augmented_scratch

data_path: "data/processed/augmented"

model:
  name: alexnet
  pretrained: false

training:
  scratch: true
  ht: false
  params:
    epochs: 50
    batch_size: 64
    lr: 0.001
  
best_config_run: false #use in hyperparameter tuning

output_dir: results/classifier_on_augmented_ALEXNET/no_ht
